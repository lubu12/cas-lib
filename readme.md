# Library for CAS

```
// Logger
// Initialize Log when application starts
const { Log } = require('cas-lib');
const { Log } = Log.initialize(YOUR_LOG_FILE_PATH);

// jwt sign
const { jwt } = require('cas-lib');
```
